# Force rebuild: 1
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
httpx>=0.25.0
torch>=2.2.0
torchvision>=0.17.0
Pillow>=10.0.0

# 数据文件支持
openpyxl>=3.1.0  # Excel 读写
py7zr>=0.20.0    # 7Z 压缩包支持
pyarrow>=14.0.0  # Parquet 读写（HuggingFace 数据集常用格式）

# 可选依赖 - 增强模型支持
xgboost>=2.0.0
lightgbm>=4.1.0
scikit-optimize>=0.9.0
modal>=0.64.0

# Modal 远端 GPU 训练后端（参考 Synapse-agent 的做法）
# 真正的 PyTorch / cv2 / transformers 等重依赖跑在 Modal 镜像里，
# 见 backend/codegen/modal_functions.py。本地容器装的 torch 是给 QA pipeline
# 自己跑单元测试用的——不是给训练用的。

# 测试
pytest>=7.4.0
hypothesis>=6.90.0
